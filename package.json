{
  "name": "notch-zerops-brain",
  "version": "0.1.0",
  "private": true,
  "description": "Brain — see your Zerops architecture, find what your repo needs and your project is missing, and provision it",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.test.json",
    "build": "tsc",
    "test": "vitest run",
    "migrate": "tsx src/db/migrate.ts",
    "dev": "tsx src/server.ts",
    "start": "node dist/server.js",
    "check": "tsx src/cli/check.ts"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "pg": "^8.22.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/pg": "^8.20.4",
    "tsx": "^4.23.8",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
